#
# Teacher routes.
#
# This file contains all routes to the teacher accessible pages.
#
teacher_index:
  path: /{_locale}/teacher
  controller: App\Controller\TeacherController::index
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_participators:
  path: /{_locale}/participators
  controller: App\Controller\TeacherController::participators
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_chatMonitor:
  path: /{_locale}/chatmonitor
  controller: App\Controller\TeacherController::chatMonitor
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_loadEvent:
  path: /{_locale}/loadEvent
  controller: App\Controller\TeacherController::loadEvent
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_createSession:
  path: /{_locale}/createSession
  controller: App\Controller\CreateSessionController::new
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_showStatistics:
  path: /{_locale}/showStatistics
  controller: App\Controller\TeacherController::showStatistics
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_uploadSenario:
  path: /{_locale}/uploadScenario
  controller: App\Controller\UploadScenarioController::new
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_assignPoints:
  path: /{_locale}/assignPoints
  controller: App\Controller\TeacherController::assignPoints
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

teacher_startSenario:
  path: /{_locale}/startSenario
  controller: App\Controller\TeacherController::startScenario
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'

#
# Logger (Teacher) routes.
#
# This file contains all routes to the Logger accessible pages. The logger
# is a teacher that walks around during a game session and can add additional
# information to the students behaviour during the game.
#
logger_index:
  path: /{_locale}/logger
  controller: App\Controller\AdminController::index
  requirements:
    _locale: '%app.locales%'
  defaults:
    _locale: '%locale%'